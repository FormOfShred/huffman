#ifdef TEST_BUILD

#include "catch.hpp"
#include "../data/frequency-table.h"

/*TEST_CASE("Increment test")
{
    data::FrequencyTable<std::string> table;
    REQUIRE(table["a"] == 0);
    table.increment("a");
    REQUIRE(table["a"] == 1);
    table.increment("a");
    REQUIRE(table["a"] == 2);
    table.increment("b");
    REQUIRE(table["b"] == 1);
    REQUIRE(table["a"] == 2);
}

TEST_CASE("Values test")
{
    data::FrequencyTable<std::string> table;
    REQUIRE(table.values().size() == 0);
    table.increment("a");
    table.increment("b");
    REQUIRE(table.values().size() == 2);
    REQUIRE(table.values()[0] == "a");
    REQUIRE(table.values()[1] == "b");
    table.increment("a");
    REQUIRE(table.values().size() == 2);
    REQUIRE(table.values()[0] == "a");
}*/

#endif